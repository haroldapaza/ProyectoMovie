# Proyecto Final - store app
- Para correr el proyecto coloca en el archivo `.env` en el valor `REACT_APP_API_KEY` el apikey que generaste en el avance 2 del proyecto.
- Posterior, ejecuta `npm install` en la terminal.
